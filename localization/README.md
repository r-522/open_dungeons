# Localization

Source of truth: `strings.csv`. Godot generates `.translation` files via the editor
(Project > Tools > Import Translations). Supported locales: ja, en, zh, ko, es.
Fallback: en. Keys are semantic (§16.2).
